# notes_site

This is the Quarto website for the ISA 401 class notes. It renders to
`../docs`, which GitHub Pages serves from this repo's default branch.

The `class*/` folders are written by `isa_notes/session.py --publish`; do
not edit their `index.qmd` files by hand, since the next publish overwrites
them.
